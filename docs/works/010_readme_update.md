# 010: README整備

Issue: #10

## 何をしたか

- 「プロジェクト構成」セクションを追加(主要ディレクトリと役割の一覧表)
- 「CI」セクションを追加(PR 時の lint / build / 成果物検証の内容と、`astro check`・リンク切れ検査が未導入であることを明記)
- デプロイ節の「PR ではビルド検証のみ実行される」という旧構成(deploy.yml が PR でも走っていた頃)の記述を削除し、PR チェックは `ci.yml`、デプロイは main push 時の `deploy.yml` という現行の分担に整合
- 作品(works)の front-matter 項目をブログ同様に明記

## 判断

- README は初期版が既に存在したため全面書き換えはせず、#15(PR チェック CI)以降の構成変更との不整合解消と不足セクションの追加に絞った

## 次のステップ

- 特になし(構成が変わったら随時更新)
